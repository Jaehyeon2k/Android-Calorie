# 테스트 결과 (Test Results) — 스마트 칼로리 트래커

`test_plan.md`에서 수립한 테스트 전략 및 케이스에 따라 실행한 결과를 아래 표와 같이 기록합니다.

## 1. 테스트 실행 환경
- **디바이스:** Pixel 7 Pro (Android 14, API 34, AVD Emulator)
- **테스트 일시:** 2026년 6월 14일
- **빌드 버전:** Debug (assembleDebug)
- **실행 도구:** Android Studio UI Test Runner 및 CLI (`./gradlew test connectedAndroidTest lint`)

## 2. 단위/UI 기능 테스트 결과 요약
* 총 수행 케이스: **10건** (단위 3건, 통합/UI 5건, 영속성 2건)
* 결과 통계: **PASS 10건**, FAIL 0건

### 2.1 핵심 비즈니스 로직 단위 검증 결과
| TC-ID | 테스트 시나리오 | 실행 결과 및 세부 내용 | 상태 |
|-------|----------------|----------------------|------|
| TC-UNIT-01 | 100g당 120kcal인 음식을 50.5g 섭취했다고 입력 | JUnit 테스트 수행. 120 * (50.5/100) = 60.6 계산 후 60kcal로 올바르게 변환 저장됨 확인. | **PASS** |
| TC-UNIT-02 | 빈 재료가 포함된 상태로 요리(레시피) 저장 시도 | 빈 텍스트 무시 로직 작동. 정상 재료 칼로리만 추출하여 합산됨 검증. | **PASS** |
| TC-UNIT-03 | 키 170cm, 체중 65kg, 나이 25세 남성 프로필 입력 | 예상 수치 1592.5kcal 도출 일치. `CalorieCalculatorTest.kt` 테스트 모두 통과. | **PASS** |

### 2.2 앱 통합/UI/API 기능 검증 결과
| TC-ID | 테스트 시나리오 | 실행 결과 및 세부 내용 | 상태 |
|-------|----------------|----------------------|------|
| TC-UI-01 | 식약처 API에서 '닭가슴살' 검색 시도 | 페널티 점수 스코어링 로직이 작동하여 가공식품이 리스트 하단으로 밀림. 순수 원물 닭가슴살 상단 노출 성공. | **PASS** |
| TC-UI-02 | '외계인음식' 등 API에 없는 미존재 키워드 검색 시도 | 빈 리스트(emptyList) 리턴 처리. `SnackbarHost`를 통해 오류 안내 메시지 출력 완료. | **PASS** |
| TC-UI-03 | 입력 필드 외부의 빈 공간 또는 리스트 영역 터치 | 리스트 스크롤 시 `FocusManager.clearFocus()`가 호출되며 즉시 소프트 키보드 사라짐. | **PASS** |
| TC-UI-04 | 다이어리 → 음식사전 → 레시피 → 뒤로가기 연속 조작 | `CalorieAppTest.kt` 실행 완료. `saveState`/`restoreState` 플래그 정상 작동하여 State 유실 없이 렌더링 유지됨. | **PASS** |
| TC-UI-05 | 이미지 첨부를 위해 갤러리 아이콘 터치 | Android 13+의 안전한 `PickVisualMediaRequest`가 작동하여 추가 권한 없이 사진만 피킹 성공. | **PASS** |

### 2.3 오프라인 및 영속성 테스트 (Manual E2E) 결과
| TC-ID | 테스트 시나리오 | 실행 결과 및 세부 내용 | 상태 |
|-------|----------------|----------------------|------|
| TC-DB-01 | 비행기 탑승 모드(오프라인) 진입 후 새로운 음식 추가 | API 기능 제외 모든 음식 조회/추가 정상 수행. Room DB 오프라인 퍼스트 아키텍처 이상 없음. | **PASS** |
| TC-DB-02 | 앱을 강제 종료(Swipe Kill)한 뒤 다시 실행 | ViewModel이 재생성될 때 DB에서 다시 StateFlow를 Observe하여 이전 상태 완전 복구 성공. | **PASS** |

## 3. 정적 코드 분석 (Static Analysis)
- **수행 명령어:** `./gradlew lint`
- **목적:** XML/코드 내 잠재적 오류나 비효율적인 리소스 사용, 접근성 위반 사항 탐지.
- **결과 요약:** 
  - 치명적인 오류(Error) 0건 탐지.
  - 빌드를 중단시킬만한 결함 없음 검증 완료. (Warnings만 일부 무시 처리)
