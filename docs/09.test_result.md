# 테스트 결과

## 실행 환경

- 날짜: 2026-06-01
- 프로젝트: Calorie
- 저장소: Room 로컬 DB

## 결과

| 항목 | 명령어 | 상태 | 비고 |
| --- | --- | --- | --- |
| 단위 테스트 | `./gradlew testDebugUnitTest` | 성공 | `CalorieCalculatorTest` 포함 |
| UI/Room 테스트 | `./gradlew connectedDebugAndroidTest` | 성공 | Pixel_6 AVD Android 15에서 4개 테스트 통과 |
| 정적 분석 | `./gradlew lintDebug` | 성공 | HTML 리포트 생성: `app/build/reports/lint-results-debug.html` |
| APK 빌드 | `./gradlew assembleDebug` | 성공 | `app/build/outputs/apk/debug/app-debug.apk` 생성 |
| 앱 설치/실행 | `./gradlew installDebug`, `adb shell monkey -p com.example.calorie 1` | 성공 | 에뮬레이터 설치 및 런처 실행 확인 |
| 확장 후 전체 검증 | `./gradlew testDebugUnitTest connectedDebugAndroidTest lintDebug` | 성공 | 레시피 API/UI 확장 후 회귀 검증 |

## 특이사항

- Gradle 실행 시 사용자 홈의 `.gradle` 캐시 접근이 필요했다.
- 외부 API 기능은 네트워크가 필요하지만, 핵심 CRUD는 Room에 저장되어 오프라인에서도 유지된다.
